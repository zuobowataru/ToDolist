<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:mybatis="http://mybatis.org/schema/mybatis-spring"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring.xsd
    ">

    <import resource="classpath:/META-INF/spring/todo-env.xml" />

    <!-- define the SqlSessionFactory -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <property name="configLocation" value="classpath:/META-INF/mybatis/mybatis-config.xml" />
    </bean>

    <!-- scan for Mappers -->
    <mybatis:scan base-package="com.example.todo.domain.repository" />
<!-- org.terasoluna.gfw.common.sequencer.Sequencerインタフェースを実装したクラスを、bean定義する。 -->
<bean id="articleIdSequencer" class="org.terasoluna.gfw.common.sequencer.JdbcSequencer">
     <!-- シーケンス値を取得するSQL -->
    <property name="dataSource" ref="dataSource" />
     <!--取得するシーケンス値の型を指定 -->
    <property name="sequenceClass" value="java.lang.String" />
    <!-- 次のシーケンス値を取得するためのSQLを -->
    <property name="nextValueQuery"
        value="SELECT TO_CHAR(NEXTVAL('todo_seq'),'AFM0000000000')" />
    <!-- 現在のシーケンス値を取得するためのSQLを -->
    <property name="currentValueQuery"
        value="SELECT TO_CHAR(CURRVAL('todo_seq'),'AFM0000000000')" />
</bean>
</beans>
